<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign Up - Tuwaze</title>
  </head>
  <body>
    <!-- Navigation -->
    <nav>
      <div class="nav-container">
        <a href="/" class="logo"> <span>🌱</span> Tuwaze </a>
        <ul class="nav-links">
          <li><a href="/">Home</a></li>
          <li><a href="/#features">Features</a></li>
          <li><a href="/login">Login</a></li>
        </ul>
      </div>
    </nav>

    <!-- Sign Up Container -->
    <div class="auth-container">
      <div class="auth-header">
        <h1>Create Account</h1>
        <p>
          Join Tuwaze and empower your community. Already have an account?
          <a href="/login">Login here</a>
        </p>
      </div>

      <form id="signupForm" method="POST" action="/signup">
        <!-- Personal Information -->
        <div class="form-group">
          <label for="fullname">Full Name *</label>
          <input
            type="text"
            id="fullname"
            name="fullname"
            placeholder="John Doe"
            required
          />
          <div class="error-message"></div>
        </div>

        <div class="form-group">
          <label for="email">Email Address *</label>
          <input
            type="email"
            id="email"
            name="email"
            placeholder="you@example.com"
            required
          />
          <div class="error-message"></div>
        </div>

        <div class="form-group">
          <label for="phone">Phone Number</label>
          <input
            type="tel"
            id="phone"
            name="phone"
            placeholder="+254 (0) 700 000 000"
          />
          <div class="error-message"></div>
        </div>

        <!-- Location -->
        <div class="form-group">
          <label for="location">Location/Ward *</label>
          <input
            type="text"
            id="location"
            name="location"
            placeholder="e.g., Kapseret Ward"
            required
          />
          <div class="error-message"></div>
        </div>

        <!-- Gender -->
        <div class="form-group">
          <label for="gender">Gender</label>
          <select id="gender" name="gender">
            <option value="">Select Gender</option>
            <option value="Male">Male</option>
            <option value="Female">Female</option>
            <option value="Other">Other</option>
            <option value="Prefer not to say">Prefer not to say</option>
          </select>
          <div class="error-message"></div>
        </div>

        <!-- Password -->
        <div class="form-group">
          <label for="password">Password *</label>
          <input
            type="password"
            id="password"
            name="password"
            placeholder="Enter a strong password"
            required
          />
          <div class="password-requirements">
            <strong>Password must contain:</strong>
            <ul>
              <li class="length-check">At least 8 characters</li>
              <li class="uppercase-check">At least one uppercase letter</li>
              <li class="lowercase-check">At least one lowercase letter</li>
              <li class="number-check">At least one number</li>
              <li class="special-check">
                At least one special character (!@#$%^&*)
              </li>
            </ul>
          </div>
          <div class="error-message"></div>
        </div>

        <div class="form-group">
          <label for="confirmPassword">Confirm Password *</label>
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            placeholder="Confirm your password"
            required
          />
          <div class="error-message"></div>
        </div>

        <!-- Terms & Conditions -->
        <div class="checkbox-group">
          <input type="checkbox" id="terms" name="terms" required />
          <label for="terms"
            >I agree to the
            <a href="#" style="color: var(--light-green)">Terms & Conditions</a>
            and
            <a href="#" style="color: var(--light-green)"
              >Privacy Policy</a
            ></label
          >
        </div>

        <div class="checkbox-group">
          <input type="checkbox" id="newsletter" name="newsletter" />
          <label for="newsletter"
            >Send me updates about community issues and platform features</label
          >
        </div>

        <button type="submit" class="btn-submit">Create Account</button>

        <!-- Already have account -->
        <p
          style="
            text-align: center;
            margin-top: 1.5rem;
            color: var(--gray-text);
          "
        >
          Already have an account?
          <a href="/login" style="color: var(--light-green); font-weight: 600"
            >Login here</a
          >
        </p>
      </form>
    </div>

    <!-- Footer -->
    <footer>
      <p>&copy; 2024 Tuwaze. Let's enlighten together.</p>
    </footer>

    <script>
      const form = document.getElementById("signupForm");
      const passwordInput = document.getElementById("password");
      const passwordRequirements = document.querySelectorAll(
        ".password-requirements li",
      );

      // Password validation
      passwordInput.addEventListener("input", function () {
        const password = this.value;
        const checks = {
          "length-check": password.length >= 8,
          "uppercase-check": /[A-Z]/.test(password),
          "lowercase-check": /[a-z]/.test(password),
          "number-check": /[0-9]/.test(password),
          "special-check": /[!@#$%^&*]/.test(password),
        };

        Object.entries(checks).forEach(([className, isValid]) => {
          const element = document.querySelector("." + className);
          if (isValid) {
            element.classList.add("met");
          } else {
            element.classList.remove("met");
          }
        });
      });

      // Form validation
      form.addEventListener("submit", function (e) {
        e.preventDefault();
        let isValid = true;

        // Clear previous errors
        document.querySelectorAll(".error-message").forEach((el) => {
          el.classList.remove("show");
        });
        document.querySelectorAll("input").forEach((el) => {
          el.classList.remove("error");
        });

        // Validate full name
        const fullname = document.getElementById("fullname").value.trim();
        if (!fullname) {
          showError("fullname", "Full name is required");
          isValid = false;
        }

        // Validate email
        const email = document.getElementById("email").value.trim();
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        if (!email) {
          showError("email", "Email is required");
          isValid = false;
        } else if (!emailRegex.test(email)) {
          showError("email", "Please enter a valid email address");
          isValid = false;
        }

        // Validate location
        const location = document.getElementById("location").value.trim();
        if (!location) {
          showError("location", "Location/Ward is required");
          isValid = false;
        }

        // Validate password
        const password = document.getElementById("password").value;
        if (!password) {
          showError("password", "Password is required");
          isValid = false;
        } else if (
          !/[A-Z]/.test(password) ||
          !/[a-z]/.test(password) ||
          !/[0-9]/.test(password) ||
          password.length < 8
        ) {
          showError("password", "Password does not meet requirements");
          isValid = false;
        }

        // Validate confirm password
        const confirmPassword =
          document.getElementById("confirmPassword").value;
        if (password !== confirmPassword) {
          showError("confirmPassword", "Passwords do not match");
          isValid = false;
        }

        // Validate terms
        if (!document.getElementById("terms").checked) {
          alert("You must agree to the Terms & Conditions");
          isValid = false;
        }

        if (isValid) {
          // Here you would normally submit the form
          //alert("Account created successfully!");
          form.submit();
        }
      });

      function showError(fieldId, message) {
        const field = document.getElementById(fieldId);
        const errorDiv = field.nextElementSibling;
        field.classList.add("error");
        errorDiv.textContent = message;
        errorDiv.classList.add("show");
      }
    </script>
  </body>
</html>